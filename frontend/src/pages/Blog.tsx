// import { useBlogs } from "../hooks"

export const Blog = () => {
    return <div>
        blog blog
        {/* const { id } = useParams();
        const { loading, blog } = useBlogs({
        }) */}
    </div>
}